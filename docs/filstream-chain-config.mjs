/**
 * Defaults embedded in meta.json for viewer donate flow.
 * Replace token `address` with your deployed USDFC (or bridged stable) on the target chain.
 */
export const USDFC_DONATE_TOKEN = {
  symbol: "USDFC",
  address: "0x0000000000000000000000000000000000000000",
  decimals: 18,
  chainId: 314159,
  chainName: "Filecoin Calibration",
};
